name: Falco CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: falco-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "0.12.8"
          enable-cache: true
      - name: Verify lockfile
        run: uv lock --check
      - name: Install locked dependencies
        run: uv sync --locked
      - name: Static checks
        run: uv run ruff check .
      - name: Unit and API tests
        run: uv run pytest tests/unit tests/api

  migrations:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: rag_db
          POSTGRES_USER: rag_user
          POSTGRES_PASSWORD: falco-ci-password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U rag_user -d rag_db"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      POSTGRES_DATABASE_URL: postgresql+psycopg2://rag_user:falco-ci-password@localhost:5432/rag_db
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "0.12.8"
          enable-cache: true
      - run: uv sync --locked --no-dev
      - name: Upgrade fresh database
        run: uv run alembic upgrade head
      - name: Exercise downgrade and re-upgrade
        run: |
          uv run alembic downgrade base
          uv run alembic upgrade head

  containers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Prepare reference environment
        run: cp .env.example .env
      - name: Validate Compose
        run: docker compose config --quiet
      - name: Smoke database bootstrap
        run: |
          docker compose up -d postgres
          docker compose run --rm airflow-db-init
          docker compose exec -T postgres psql -U rag_user -d airflow_db -c 'SELECT 1'
          docker compose run --rm --build migrate
          docker compose exec -T postgres psql -U rag_user -d rag_db -c 'SELECT version_num FROM alembic_version'
      - name: Build Falco API image
        run: docker build -t falco-agentic-rag:ci .
      - name: Build Falco Airflow image
        run: docker build -t falco-airflow:ci ./airflow
      - name: Tear down smoke resources
        if: always()
        run: docker compose down -v --remove-orphans
